variable "ecs_cluster_name" {
  type    = string
  default = "my-cluster"  # Replace with your ECS cluster name or provide as input
}

variable "ecs_service_name" {
  type    = string
  default = "my-service"  # Replace with your ECS service name or provide as input
}
